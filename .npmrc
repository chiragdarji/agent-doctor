# Default registry — npmjs.com
registry=https://registry.npmjs.org/

# GitHub Packages — uncomment and set GITHUB_TOKEN to publish there
# @chiragdarji:registry=https://npm.pkg.github.com
# //npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}
