# Default registry — npmjs.com
registry=https://registry.npmjs.org/

# GitHub Packages (opt-in — set GITHUB_TOKEN and use --registry to publish there)
# @chiragdarji:registry=https://npm.pkg.github.com
# //npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}
